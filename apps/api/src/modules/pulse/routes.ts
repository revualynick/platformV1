import type { FastifyPluginAsync } from "fastify";

export const pulseRoutes: FastifyPluginAsync = async (app) => {
  // Pulse check module — Phase 5
  // Listens to comms signals and triggers organic follow-up conversations
};
