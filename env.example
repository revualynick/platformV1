# ─────────────────────────────────────────────────────────
# Revualy — Environment Variables
# Copy this file to .env and fill in the values.
# Per-tenant instances need all sections except [Demo Mode].
# The marketing/demo site needs all sections.
# ─────────────────────────────────────────────────────────

# ── Tenant Identity ──────────────────────────────────────
ORG_ID=                          # UUID — unique org identifier
ORG_NAME=                        # Human-readable org name (e.g. "Acme Corp")

# ── Docker Compose (local dev only) ─────────────────────
POSTGRES_USER=revualy
POSTGRES_PASSWORD=                    # Set a local dev password
POSTGRES_DB=revualy_dev
REDIS_PASSWORD=                       # Set a local dev password

# ── Database ─────────────────────────────────────────────
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# ── Redis ────────────────────────────────────────────────
REDIS_URL=redis://:password@host:6379

# ── Auth (NextAuth.js) ──────────────────────────────────
NEXTAUTH_URL=https://acme.revualy.com    # Full URL of this instance
NEXTAUTH_SECRET=                          # openssl rand -base64 32

# ── Internal API Secret ──────────────────────────────────
# Shared secret between Next.js (web) and Fastify (api).
# Used for x-internal-secret header validation.
INTERNAL_API_SECRET=             # openssl rand -base64 32

# ── API URL (web → api) ─────────────────────────────────
INTERNAL_API_URL=http://localhost:3000    # In Railway: use private networking URL

# ── Google OAuth ─────────────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── AI (LLM) ────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Email ────────────────────────────────────────────────
RESEND_API_KEY=

# ── Demo Mode (marketing site only) ─────────────────────
# DEMO_MODE=true                 # Enables lead-gated demo chat, bypasses auth on demo routes

# ── Optional: Public API URL (client-side demo chat) ────
# NEXT_PUBLIC_API_URL=https://revualy.com   # Only needed if demo chat calls API directly
